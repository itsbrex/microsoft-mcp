---
description: Run the microsoft-mcp server locally
allowed-tools: ["Bash(uv run microsoft-mcp:*)"]
---

Start the MCP server via its entry point. Set `MICROSOFT_MCP_AUTH_METHOD=msal` and `MICROSOFT_MCP_ACCOUNT_ID=<email>` first if you want MSAL mode.

!`uv run microsoft-mcp`
